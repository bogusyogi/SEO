# SEO third-party notices

This package was extracted from `Orthic-Labs/legion` at
`a4eaaa223c284ab81641c4283903648a2a8c1f14`; its original project license is retained
in `LICENSE`. This is provenance, not a runtime dependency. Third-party components
remain governed by their own licenses and are not restricted by the root terms.

The earlier inventory overstated how much material was original. Historical SEO
scripts and references include material derived from `AgriciDaniel/claude-seo` and
its Banana extension (MIT, copyright 2026 agricidaniel). The restored image helpers
and PDF reference were recovered from the exact source pin above. Preserve the
upstream MIT notice in `licenses/claude-seo-MIT.txt` with distributions.

`every-app/open-seo` and `seranking/seo-skills` were consulted for project context,
provider semantics, comparable historical observations and completed-workflow
contracts. No SaaS shell, hosted service, billing implementation or SE Ranking
client is imported as a required runtime dependency.

`harlan-zw/unlighthouse` and `seo-skills/seo-audit-skill` (SEOmator) are optional,
separately installed external tools, not vendored binaries. Their own licenses and
runtime requirements apply. The adapter does not download them or claim their
results are first-party field measurements.

`best-of-ai/awesome-ai-seo` and `EinGuterWaran/awesome-free-seo-backlinks` are discovery
references only. Their lists and asserted authority scores are not copied into this
package. License ambiguity and unverified claims must not be bypassed by wholesale
import. See `docs/DONOR-REVIEW.md` for reviewed file revisions and decisions.

Python dependencies are declared in requirements.txt and retain their upstream
licenses. An explicit runtime setup installs them into a separate virtual environment.
No font files, API credentials, private site data or external model weights are bundled.
