---
name: seo
description: Manage technical SEO, content, backlinks, rankings, GSC, GA4 and recurring site operations with the independent SEO runtime.
---

Read `../../SKILL.md` relative to this file. It is the canonical operating
contract. The complete plugin must be installed, not this forwarding file alone.
Resolve the package root as this directory's grandparent. Invoke
`python <absolute-package-root>/seo.py --help`. Resolve scripts and references
from the package root, never from the audited site's working directory.
Keep project state in the selected site's `.seo/`, not the plugin cache.

No Legion installation, particular model or paid data subscription is required.
The harness may optionally use its available writing/coding/design roles,
including Legion roles. Missing roles must not block deterministic SEO operations.
